## [1.0.23] - 2026-08-13

### 修复：历史会话回放也显示「思考过程」折叠框
- 之前只有新生成的回复有「思考过程」框，重新打开历史会话时旧消息直接渲染成结果框，看起来像“没有思维链”。
- 现在历史回放与实时生成保持一致：每条 AI 回复上方都有可折叠的「思考过程」框，模型没有思考内容时显示「（本回复无思考过程）」占位。

---## [1.0.22] - 2026-08-13

### 重构：回复结构 = 折叠「思考过程」框 + 单个结果框
- **过程文本不再刷成结果框**：模型执行中的每步说明（“收到，我马上搜索…”）之前会一条条显示成回复气泡，现在全部收进同一个可折叠的「思考过程」框；一轮任务结束只显示一个结果框。历史会话回放同样按“过程/结果”分组渲染。
- **思考面板多行追加**：推理内容 + 每步过程说明按行追加（虚线分隔），不再互相覆盖、不再只显示最后一句。
- **机制级防中断**：模型只回确认/承诺话术（“收到、我马上做…”）却没调用工具时，插件自动丢弃该回复并向模型推送“立即继续执行”指令（最多自动催 2 次），不再需要用户反复发消息催促；任务确已完成的场景模型会直接给出最终结果。

---# Cagent 更新日志

## [1.0.21] - 2026-08-12

### 修复与优化
- **「回到最新」按钮改为输入行常驻按钮**：不再用悬浮按钮（移动端上经常不显示），现在固定在发送按钮左边，随时可点。
- **回复结构固定为「思考过程框 + 回复内容」**：每次生成回复时，先出现一个折叠的「思考过程」框（点击展开），下方是回复内容；模型没有返回思考时会显示「（本回复无思考过程）」占位。

---
## [1.0.20] - 2026-08-12

### 修复与体验
- **修复：AI 只回一句「收到」就中断**。两处原因一起修：① 模型回复被 max_tokens 截断时现在会自动续写（最多 2 次），不再留下半句话；② 系统提示词明确禁止只回确认、必须立即调用工具执行到底。同时把「最大输出 token」默认值从 4096 提到 8192（推理模型建议保持 8192+）。
- **修复：聊天里「回到最新」按钮消失**。根因是渲染历史/欢迎页时会清空消息列表，把按钮 DOM 一起删掉了。现在每次清空后自动重建按钮。
- **思维链 / 工具调用展示**：「显示工具调用」现在默认开启；生成回复时「思考过程」面板显示在回复上方（可点击展开/收起），工具调用面板同样可折叠；思考过程会随会话保存，重新打开历史聊天仍可展开查看。

---
## [1.0.19] - 2026-08-10

### 修复：create_plot 奈奎斯特图竖线 + 传递函数解析错误
- **奈奎斯特图竖线（主要问题）**：修复坐标量程算法。含积分环节的系统（s 在分母）在 ω→0 时虚部趋于无穷，旧算法按最大值定标，把实部量程压成亚像素，导致曲线缩成一条竖线。现在按中位数定标、尾迹画出画布外并裁剪（教科书式画法），伯德/奈奎斯特曲线形状恢复正常。
- **传递函数解析器 s^n 指数错误**：`s^2` 之前被解析成 `s^3`（多乘了一次 s），导致所有含幂次写法（如 `s^3+20s^2+75s`）的系统算错。已修复并支持 `(s+1)^2` 展开。
- **随机回归**：200 组随机传递函数（0/1/2 型）验证无 NaN、无退化竖线。

---
## [1.0.18] - 2026-08-10

### 新增：create_plot 画图工具
- **精确计算曲线**：伯德图（幅频/相频）、奈奎斯特图、根轨迹、阶跃响应，由插件用复数运算解析传递函数后计算绘制，AI 不再手画示意图。
- **结构图**：方框图、信号流图自动生成 Mermaid 或 Excalidraw 源码，AI 直接在笔记中输出。
- **自动插入**：可配置绘图结果自动写入指定文件夹（默认 Cagent-Diagrams）并链接到当前笔记。
- **新增设置项**：图表文件夹、图表格式（Mermaid/Excalidraw）、自动插入开关。

---

## [1.0.17] - 2026-08-10

### 界面重构（参考 Chatbox 风格）
- **消息布局**：每条消息改为「头像 + 名字 + 时间」信息行在上、气泡在下、复制按钮在气泡下方的纵向结构，不再左右横排。
- **气泡**：用户和 AI 统一浅灰柔和底色 + 大圆角（14px），白底界面下层次清晰，不再白底白字看不清。
- **输入框**：改为浅灰大圆角容器，聚焦时显示强调色光环。
- **流式消息**：思考光标移入气泡内部，打字时视觉更连贯。

---

## [1.0.16] - 2026-08-10

### 优化（聊天可读性）
- **消息间距**：消息与消息间距加大（10px → 14px），气泡内边距与行高提升。
- **内容排版**：段落、标题、列表、代码块、引用、表格、图片的间距单独细化，长回答不再挤成一团。

---

## [1.0.15] - 2026-08-06

### 优化（按钮布局）
- **头部精简**：6 个操作按钮收进「更多」菜单（新对话 / 搜索 / 会话管理 / 导出 / 清空），只留模型切换 + 更多两个按钮，窄屏不再拥挤。
- **「回到最新」按钮归位**：从输入框上方移到消息列表右下角，不再与发送按钮重叠。
- **移动端触控**：输入行相机 / 图片 / 发送按钮统一 40px 圆形触控目标。

---

## [1.0.14] - 2026-08-06

### 优化（视觉升级，借鉴 Copilot 聊天界面）
- **消息气泡**：更大圆角（16px）+ 柔和阴影，AI 气泡浅色混底，用户气泡保留强调色，阅读更接近 Copilot。
- **输入区**：大圆角容器，聚焦时显示强调色光环；发送按钮改为圆形，相机/图片按钮圆形触控。
- **头像与欢迎页**：AI 头像带投影，欢迎 Logo 渐变背景 + 光晕，提示卡片 hover 微动效。
- **代码块/折叠面板/搜索框**：统一圆角与边框，视觉层次更清晰。

---

# Cagent 更新日志

## [1.0.13] - 2026-08-06

### 新增（半屏 / 全屏）
- **对话打开方式可选半屏 / 全屏**：设置「对话打开方式」下拉改为「半屏（侧边栏）」/「全屏（标签页）」。
- **移动端始终全屏（默认开）**：手机上打开聊天强制使用全屏标签页，不再出现半屏抽屉；可在设置中关闭以恢复侧边栏方式。若聊天已半屏打开，会自动迁移到全屏，不会重复开窗口。

### 优化（移动端 UI，借鉴 Copilot 布局方案）
- **底部导航栏适配**：输入框不再被 Obsidian 移动端底部导航栏/刘海屏遮挡（自动预留安全区高度）。
- **软键盘弹出自动隐藏头部**：打字时头部隐藏，把整屏让给消息区和输入框。
- **窄屏布局**：隐藏次要的 token 计数、操作按钮加大触控目标且可横向滚动、消息气泡更宽、输入框 16px 避免 iOS 聚焦自动缩放。
- **触屏优化**：触屏设备不再依赖 hover 高亮。

---

## [1.0.12] - 2026-08-04

### 新增（上下文管理）
- **工具调用不再显示**：默认隐藏工具调用面板和历史里的工具气泡（可在设置「对话显示」里重新开启）。
- **精简工具调用记录**：发送时丢弃历史里的工具消息和 tool_calls，只保留最终文字。工具结果往往是 token 大头，精简后省 token 且不影响缓存命中。
- **滑动窗口（可选，默认关）**：设置里可限制"只发送最近 N 条消息"。默认关闭——因为开启会降低 Prompt 缓存命中率（前缀频繁变化），仅在上下文过大时使用。
- 底部 token 估算现在反映**实际发送量**（含精简与窗口），能看到省了多少。

---

## [1.0.11] - 2026-08-04

### 新增
- **多模型配置**：设置界面新增「模型配置」区块，可预设多个配置（名称 + 接口地址 + API Key + 模型），切换自如。
- **聊天界面切换模型按钮**：头部新增模型按钮，弹出菜单列出所有配置；激活项带勾，**未填 API Key 的配置灰色不可选**。
- **API Key 测试按钮**：每个配置的 API Key 旁有「测试连接」按钮，验证 key/接口/模型组合（可区分 key 错误、地址错误、网络错误）。
- **同一个模型保存两个 API Key**：如 deepseek-v4-flash 可同时建「DeepSeek 原生」和「Coding Plan」两个配置，同模型不同 key，随时切换。

### 变更
- 设置界面的模型配置从"单个预设下拉"升级为"多配置管理"，老用户的现有设置自动迁移为默认配置，不丢失。

---

## [1.0.10] - 2026-08-04

### 新增
- **联网搜索功能**：新增 `web_search` 工具，AI 可搜索网页并总结结果（搜题、查知识点）。基于 Tavily API。
- **设置界面新增「联网搜索」配置**：可开关联网搜索 + 填写 Tavily API Key（tavily.com 注册，免费每月 1000 次）。

---

## [1.0.9] - 2026-08-04

### 性能
- **发送前剥离冗余字段**：历史消息里的 `ts`、`prompt`、`images`（海量 base64 图片数据）不再序列化进请求体——它们对主模型无用却烧钱、且破坏缓存前缀稳定。现在发送给 API 的是纯净的标准字段，大幅降低 token 消耗、提升缓存命中。
- **缓存命中率显示**：底部状态栏现在显示 `cache X%`（基于 DeepSeek 的 `prompt_cache_hit_tokens` / `prompt_cache_miss_tokens`，兼容 OpenAI `prompt_tokens_details.cached_tokens`），可直接看到优化效果。

## [1.0.8] - 2026-08-04

### 性能
- **Prompt 缓存命中率优化**：把动态时间戳从系统提示词最开头移到末尾。此前时间戳每秒变化、位于缓存前缀起点，导致每次请求缓存全 miss（命中率 ~0%）；现在静态的 BASE_PROMPT + memory + skills 组成稳定前缀可命中缓存，大幅降低 API 费用并提速。

---

## [1.0.7] - 2026-08-03

### 新增
- **回到最新消息按钮**：长对话中滚动远离底部时，右下角出现悬浮按钮，点击直达最新消息。加载历史会话后自动跳到底部。

---

## [1.0.6] - 2026-08-03

### 修复
- **AI 无法读取当前日期时间**：在系统提示词中注入当前时间（`<Current time>`），模型现在知道"现在几点/几号"。
- **`tool` 消息角色顺序错误（400 错误）**：新增 `sanitizeMessages()` 清洗函数，发送前自动丢弃"孤儿" tool 消息、裁剪不完整的工具调用回合，避免 DeepSeek/Qwen 接口因角色顺序错乱返回 400。

### 新增
- **用户消息复制按钮**：用户发出的消息下方也显示复制按钮（此前只有 AI 回复有）。

### 分发
- README 新增 **BRAT 安装方式**（官方社区库审核通过前，可通过 BRAT 从 GitHub Release 安装）。

---

## [1.0.5] - 2026-08-02

### 修复
- 图片查看器误报"请打开视觉模型"。
- 识图流程中 pending 图片被提前清空导致识图不生效。

### 新增
- 拍照按钮（调用手机后置摄像头）。
- 聊天记录图片点击放大查看。
- 消息时间分组、AI 流式打字光标、空对话欢迎页、自定义头像（用户/AI）。
- 消息操作栏（移动端可点）。

### 变更
- 默认视觉模型升级为 `qwen-vl-max`。
