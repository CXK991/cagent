# Cagent 更新日志

## [1.0.14] - 2026-08-06

### 优化（视觉升级，借鉴 Copilot 聊天界面）
- **消息气泡**：更大圆角（16px）+ 柔和阴影，AI 气泡浅色混底，用户气泡保留强调色，阅读更接近 Copilot。
- **输入区**：大圆角容器，聚焦时显示强调色光环；发送按钮改为圆形，相机/图片按钮圆形触控。
- **头像与欢迎页**：AI 头像带投影，欢迎 Logo 渐变背景 + 光晕，提示卡片 hover 微动效。
- **代码块/折叠面板/搜索框**：统一圆角与边框，视觉层次更清晰。

---

# Cagent 更新日志

## [1.0.13] - 2026-08-06

### 新增（半屏 / 全屏）
- **对话打开方式可选半屏 / 全屏**：设置「对话打开方式」下拉改为「半屏（侧边栏）」/「全屏（标签页）」。
- **移动端始终全屏（默认开）**：手机上打开聊天强制使用全屏标签页，不再出现半屏抽屉；可在设置中关闭以恢复侧边栏方式。若聊天已半屏打开，会自动迁移到全屏，不会重复开窗口。

### 优化（移动端 UI，借鉴 Copilot 布局方案）
- **底部导航栏适配**：输入框不再被 Obsidian 移动端底部导航栏/刘海屏遮挡（自动预留安全区高度）。
- **软键盘弹出自动隐藏头部**：打字时头部隐藏，把整屏让给消息区和输入框。
- **窄屏布局**：隐藏次要的 token 计数、操作按钮加大触控目标且可横向滚动、消息气泡更宽、输入框 16px 避免 iOS 聚焦自动缩放。
- **触屏优化**：触屏设备不再依赖 hover 高亮。

---

## [1.0.12] - 2026-08-04

### 新增（上下文管理）
- **工具调用不再显示**：默认隐藏工具调用面板和历史里的工具气泡（可在设置「对话显示」里重新开启）。
- **精简工具调用记录**：发送时丢弃历史里的工具消息和 tool_calls，只保留最终文字。工具结果往往是 token 大头，精简后省 token 且不影响缓存命中。
- **滑动窗口（可选，默认关）**：设置里可限制"只发送最近 N 条消息"。默认关闭——因为开启会降低 Prompt 缓存命中率（前缀频繁变化），仅在上下文过大时使用。
- 底部 token 估算现在反映**实际发送量**（含精简与窗口），能看到省了多少。

---

## [1.0.11] - 2026-08-04

### 新增
- **多模型配置**：设置界面新增「模型配置」区块，可预设多个配置（名称 + 接口地址 + API Key + 模型），切换自如。
- **聊天界面切换模型按钮**：头部新增模型按钮，弹出菜单列出所有配置；激活项带勾，**未填 API Key 的配置灰色不可选**。
- **API Key 测试按钮**：每个配置的 API Key 旁有「测试连接」按钮，验证 key/接口/模型组合（可区分 key 错误、地址错误、网络错误）。
- **同一个模型保存两个 API Key**：如 deepseek-v4-flash 可同时建「DeepSeek 原生」和「Coding Plan」两个配置，同模型不同 key，随时切换。

### 变更
- 设置界面的模型配置从"单个预设下拉"升级为"多配置管理"，老用户的现有设置自动迁移为默认配置，不丢失。

---

## [1.0.10] - 2026-08-04

### 新增
- **联网搜索功能**：新增 `web_search` 工具，AI 可搜索网页并总结结果（搜题、查知识点）。基于 Tavily API。
- **设置界面新增「联网搜索」配置**：可开关联网搜索 + 填写 Tavily API Key（tavily.com 注册，免费每月 1000 次）。

---

## [1.0.9] - 2026-08-04

### 性能
- **发送前剥离冗余字段**：历史消息里的 `ts`、`prompt`、`images`（海量 base64 图片数据）不再序列化进请求体——它们对主模型无用却烧钱、且破坏缓存前缀稳定。现在发送给 API 的是纯净的标准字段，大幅降低 token 消耗、提升缓存命中。
- **缓存命中率显示**：底部状态栏现在显示 `cache X%`（基于 DeepSeek 的 `prompt_cache_hit_tokens` / `prompt_cache_miss_tokens`，兼容 OpenAI `prompt_tokens_details.cached_tokens`），可直接看到优化效果。

## [1.0.8] - 2026-08-04

### 性能
- **Prompt 缓存命中率优化**：把动态时间戳从系统提示词最开头移到末尾。此前时间戳每秒变化、位于缓存前缀起点，导致每次请求缓存全 miss（命中率 ~0%）；现在静态的 BASE_PROMPT + memory + skills 组成稳定前缀可命中缓存，大幅降低 API 费用并提速。

---

## [1.0.7] - 2026-08-03

### 新增
- **回到最新消息按钮**：长对话中滚动远离底部时，右下角出现悬浮按钮，点击直达最新消息。加载历史会话后自动跳到底部。

---

## [1.0.6] - 2026-08-03

### 修复
- **AI 无法读取当前日期时间**：在系统提示词中注入当前时间（`<Current time>`），模型现在知道"现在几点/几号"。
- **`tool` 消息角色顺序错误（400 错误）**：新增 `sanitizeMessages()` 清洗函数，发送前自动丢弃"孤儿" tool 消息、裁剪不完整的工具调用回合，避免 DeepSeek/Qwen 接口因角色顺序错乱返回 400。

### 新增
- **用户消息复制按钮**：用户发出的消息下方也显示复制按钮（此前只有 AI 回复有）。

### 分发
- README 新增 **BRAT 安装方式**（官方社区库审核通过前，可通过 BRAT 从 GitHub Release 安装）。

---

## [1.0.5] - 2026-08-02

### 修复
- 图片查看器误报"请打开视觉模型"。
- 识图流程中 pending 图片被提前清空导致识图不生效。

### 新增
- 拍照按钮（调用手机后置摄像头）。
- 聊天记录图片点击放大查看。
- 消息时间分组、AI 流式打字光标、空对话欢迎页、自定义头像（用户/AI）。
- 消息操作栏（移动端可点）。

### 变更
- 默认视觉模型升级为 `qwen-vl-max`。
